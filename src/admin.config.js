// list of the valid users for the base version of the app:

const Admin = [
    // admin list
];

export const reservedUsernames = [
    // reserved usernames
];

export const verfiedUsers = [
    // verified users
];


export default Admin;
